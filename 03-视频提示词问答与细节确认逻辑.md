# 视频提示词 · 问答式信息收集与细节确认逻辑（SOP v1.2）

> 定位：本项目（全业态电商 AI 视频）的**提示词输入逻辑层**。
> 配套：01 对照表（知识库）、02 分镜表（输出层）、04 话术（对客执行层）、05 类型库、06 合规表、knowledge/（JSON 知识库）。
> 状态：**v1.2**（复核修正：质检引用 M9、资产清单更新；类型推荐权重品类化见 05 §4 与 types.json）。

---

## 1. 缘起：一个必须回答的问题

**用户通常会给参考图。那视频提示词里，还要不要写衣服细节？写多了会不会导致变形/不符合原样？**

结论：

1. 静态外观（版型/颜色/印花/五金）→ **交给参考图，提示词零描述或一句话锁定**。
2. 面料动态（垂坠/飘动/光泽流动）→ **必须写进提示词**，这是防"丝绸变纯棉"的关键。
3. 关键材质 → **一句话锚定，不堆砌参数**。
4. 参考图不是直接采信 → **AI 先识别，识别报告交用户确认修改，确认后才成为事实**。
5. 任何写进提示词的细节，**必须经过用户确认门**才允许生成。

---

## 2. 双通道信息模型（为什么细节写多了会变形）

图生视频（Seedance 模式）中，模型同时吃两个通道：

```
参考图通道（静态）    →  版型、颜色、图案、五金、材质观感
提示词通道（动态）    →  动作、运镜、光线、场景、面料动态、画质
```

提示词里出现与参考图**重复或冲突**的外观细节时，模型尝试"融合"两路信息，产生三种失败模式：

| 失败模式 | 现象 | 原因 |
|---|---|---|
| 混血变形 | 衣服"半像参考图半像描述" | 文字细节与图片冲突，模型强行融合 |
| 文字覆盖 | 衣服变成"提示词版"，不像原品 | 描述权重压过参考图 |
| 材质错判 | 丝绸被当成纯棉来"动" | 静态图传达不了动态质感，模型按语义猜测 |

**核心结论：细节不是写错了，是写错了通道。**

---

## 3. 设计原则（三层规则）

### 原则一：通道分工
**能交给参考图的，绝不写进提示词；提示词只写参考图无法传达的东西。**

### 原则二：提示词三层结构

| 层 | 内容 | 写法 | 示例 |
|---|---|---|---|
| L1 锚定层 | 锁定外观继承 | 开头一句话，全局唯一 | `与参考图完全一致` |
| L2 动态层 | 动作/运镜/光线/场景/面料动态 | 提示词主力，详尽 | `她转身，裙摆如液体般垂坠流动` |
| L3 材质锚定层 | 面料类型 + 动态特性 | 一句话，不展开 | `丝绸面料，光泽随动作流动` |

### 原则三：禁止项
- ❌ 版型尺寸数字、色值参数、图案细节堆砌、五金件数量 → 参考图的事
- ✅ 面料类型、面料动态行为、光泽质感形容词 → 提示词的事
- ⚠️ 冲突检测：提示词与参考图矛盾（参考图长裙、文字写短裙）→ 拦截并提示，不直接生成

---

## 4. 流程总览

```
A0 商品信息卡 → A 视觉API接入检查 → B 识别+确认门 → C 自动分档
→ D 细节确认门（确认单）→ E 生成+质检 → F 批量衔接（确认单=入场券）
```

### 4.1 执行纪律（铁律，违反=流程违规）

> 本 SOP 的确认门是**对话暂停点**：每一步展示设计后必须暂停等待用户确认，确认后才进入下一步。

1. **确认门不可跳过**：识别报告、类型推荐、分镜设计、每镜提示词、细节确认单——每个设计产出都必须展示给用户并等待确认，**不得模拟、不得自动通过、不得静默继续**。
2. **测试/演示同样走真实确认**：验证流程时也要逐站确认，不允许用"测试模式"跳过交互（测试目标是流程本身，不只是代码）。
3. **未确认不进下一步**：用户未回复确认前，禁止进入后续阶段（与"确认单=队列入场券"同理，对话层也守同一规则）。
4. **展示要完整**：每个确认点展示完整设计（报告/推荐/分镜/提示词全文），不摘要、不隐藏，让用户看得见再决定。
5. **用户可随时修正**：确认时用户指出修改 → 修正后重新展示该站设计，再确认。

**违反示例**（曾犯）：识别后直接生成视频、跳过确认门——流程违规，需重走。

---

## 5. 阶段 A：API 接入检查（视觉识别 + 视频生成双通道）

### A1 视觉识别 API 接入检查（已定）

**识别能力：独立视觉识别模型 API（客户接入）。**

- 客户需提供：API Key（GPT-4o / Gemini / Claude 等支持图片输入的多模态模型）+ 模型名
- **密钥配置：统一走 `.env`（见 CONFIG.md），禁止硬编码；接单前先跑配置自检**
- 未接入 → 提醒接入；不愿接入 → 人工模式降级（明确标注识别质量下降风险）
- 识别调用：每张参考图 1 次调用，单图 + 固定识别指令模板（类型/特性/置信度/质量）
- 置信度规则：材质推断默认"中置信必确认"，其余按字段可验证性标定

### A2 视频生成模型接入检查（v1.2 新增：开放接入）

**生成模型不锁定单一厂商，开放自由接入；客户未指定时默认 Seedance 2.0。**

- 模型注册表：`knowledge/models.json`（预置 Seedance 2.0/2.5、可灵、即梦、Runway、Vidu + 自定义接入）
- **密钥配置：统一走 `.env`（见 CONFIG.md）：`VIDEO_PROVIDER` 选模型 + `{MODEL}_API_KEY/BASE/MODEL` 填密钥；缺省 VIDEO_PROVIDER=seedance-2.0**
- 客户选择：默认 Seedance 2.0（即梦开放平台）→ 或指定其他模型 → 或自定义接入（提供 API Key + Base URL + 模型名）
- **能力参数动态影响流程**（查 models.json → capability_impact）：
  - `ref_images` → B0 收图上限（9张/30张/按实际）
  - `duration` → 分镜表时长区间（4-15s 用 9 镜；支持 30s 可做长版）
  - `chinese_prompt` → 提示词语言（原生优=全中文；一般=建议中英对照确认）
  - `image_to_video` → 生成模式（支持=图生视频为主；不支持=首帧图+文生视频降级）
  - `first_frame_anchor` → 首帧锚定一致性策略是否可用
  - `consistency_strength` → 影响档位建议（弱一致性模型避免 B 档以下）

**对客户提示（接入时必发）：**

```
视频生成模型支持开放接入，默认使用 Seedance 2.0（商品一致性表现强、中文提示词原生支持）。
如需换模型：提供模型名 + API Key 即可，我们将按该模型能力适配流程（参考图上限/时长/语言/模式）。

能力差异提示：
- 商品一致性：Seedance 系 > Vidu/可灵 > Runway（服装细节易漂移）
- 中文提示词：国产模型原生优；Runway 等建议中英对照
- 参考图上限：Seedance 2.5 支持 30 图，其余多不支持多图参考
- 单次时长：Seedance 2.5 支持 30s，其余多 ≤15s
```

**降级规则**：自定义接入的模型能力未知 → 按保守值处理（参考图上限 1 张、时长 ≤10s、中文提示词需确认、档位建议 S/A 档），并在确认单标注"模型能力未验证，一致性风险自担"。

---

## 6. 阶段 B：参考图识别与确认

### B0 收图约束
- 上限 ≤9 张（Seedance 限制）；优先：白底图 / 多角度图 / 细节特写 / 使用场景图 / 场景概念图（品类化解读：服装“使用图”=上身图，美妆=上脸图，3C=手持使用图，食品=成品/摆盘图）
- 质量预检：模糊、水印、多商品同框 → 标注要求替换

### B1 AI 识别（逐张，三组输出）

**① 图片类型**（通用，决定锚点归属）

| 类型 | 说明 | 锚点作用 |
|---|---|---|
| 白底商品图 | 纯色底、产品主体 | 外形锚点 |
| 多角度展示图 | 平铺/背面/侧面/45° | 外形+细节锚点 |
| 使用/佩戴图 | 有人或场景中使用（服装上身/美妆上脸/3C手持/食品成品） | 动态锚点 |
| 细节特写图 | 材质/包装/logo/接口/工艺局部 | 细节锚点（最高价值） |
| 场景概念图 | 氛围/环境参考 | 环境锚点 |
| 生活随拍/其他 | 非标准图 | 参考价值低，标注 |

**② 商品特性**（通用字段 + 品类字段，含置信度）

- 通用字段（全品类识别）：品类、主色/主视觉、外观形态描述、**材质推断（必确认）**、关键细节（logo/标签/接口/图案）
- 品类字段（由品类知识包 category-profiles.json 定义）：服装=版型/印花/五金；美妆=色号/质地；食品=口味/规格/形态；3C=接口/屏幕/外壳
- **材质推断三要素（通用版）**：光泽（哑/亮/珠光/金属）+ 质感（柔软/硬挺/粘稠/颗粒）+ 纹理（织纹/磨砂/光滑/拉丝）→ 输出推断依据

**③ 质量检查**：清晰度、遮挡、水印

### B2 确认交互（已定：文本表格 + 自然语言回复）

- 报告以**文本表格**呈现（任何渠道可发、可归档）
- 客户用**自然语言整体回复**：回复"全部正确"，或指出"修改哪部分"（例："版型是直筒不是A字"）
- **材质/关键外观项强制确认**：客户回复"全部正确"时，若材质是推断值，需追问一次"材质确认是【推断值】吗？"（不默认为已确认）——这是全品类的命门：服装=面料、美妆=色号/质地、食品=形态/色泽、3C=外壳材质
- 修改后重发该图修订版报告，再次确认

---

## 7. 阶段 B2.5：类型推荐（商品特性驱动，v1.1 新增）

### 7.1 位置与输入输出

- **位置**：B 识别确认后、C 分档前。
- **输入**：商品信息卡（A0）确认后的 6 个特性字段：品类 / 面料 / 价格带人群 / 适用场合 / 季节 / 品牌调性。
- **输出**：推荐类型 Top3 + 推荐理由 → 客户确认锁定 → 写入确认单（D 阶段）。
- **依据**：完整特性权重表见 05 类型库 §4（本文只定义逻辑结构与规则）。

### 7.2 推荐逻辑（三层）

**第一层：硬约束（一票锁定/一票否决）**

| 规则 | 处理 |
|---|---|
| 品牌调性明确指定类型（客户说“我要复古风”） | 直接锁定，不再推荐 |
| 品类与类型不相容（童装×赛博、羽绒×国风） | 从候选池剔除 |

**第二层：打分**

每个特性字段按命中表给候选类型加分，加权求和（权重表在 05 §4）：

- 品类 → 决定候选池（基础分）
- 面料 → 质感表达需求（光泽/哑光/硬挺/飘逸/蓬松/机能）
- 价格带人群 → 调性级别（高客单→高级感类型）
- 适用场合 → 叙事场景（通勤/约会/度假/户外/居家）
- 季节 → 光线色温倾向
- 品牌调性 → 最高权重（未明确指定时按风格词匹配）

**第三层：输出**

按总分排序输出 Top3，并生成**对客可解释的推荐理由**（逐特性说明命中原因，不输出打分过程）。

### 7.3 推荐理由模板

```
根据商品信息为您推荐：
主推：① TVC 电影感大片
  理由：缎面高光泽→需光影语言（TVC/氛围）；约会场合→情感叙事（TVC/剧情）；
        轻奢价位→高级感（TVC/极简）。三因素共同指向 TVC。
备选：⑦ 情绪氛围种草 / ⑩ 极简高级感
请选择，或指定其他类型（共 12 种，见 05 类型库）。
```

### 7.4 确认交互与防漂移

- 客户选定后，类型写入确认单新增行「风格类型」；客户确认前不进入分镜表设计。
- 锁定后：该类型**镜头序列公式**作为分镜表骨架（02 模板套用），**类型特征词**作为每镜提示词统一前缀（防漂移到别的风格）。
- 客户中途想换类型 → 允许，但需重走分镜表骨架设计（镜头序列变了），提示返工成本。

---

## 8. 阶段 C：策略分档（已定：自动查表）

分档依据 B2 确认后的锚点完整度，**纯规则自动判定**：

| 档位 | 条件（确认后） | 提示词外观描述策略 |
|---|---|---|
| S | 白底 + 细节特写 + 上身 三图齐 | 零外观细节，L1 锁定，L3 只写面料动态 |
| A | 有白底 + 上身，缺细节特写 | 只补影响结果的关键项（如面料类型），逐项过确认门 |
| B | 仅 1 张图 | 补面料/版型/主色，全部过确认门 |
| C | 0 张图 | **询问式，按客户指令操作（见下）** |

### C 档处理（已定：询问式，按客户指令）

无参考图时**不替客户做决定**，给出选项让客户选：

```
当前没有参考图，可选处理方式：
1. 补真实商品图（最稳，推荐）
2. 用文字描述生成一张样衣概念图，您确认后当参考图用（转 B 档）
3. 坚持直接用文字生成（一致性无法保证，风险自负）

请告知按哪种方式继续。
```

客户选 2 → 生成样衣概念图 → 客户确认 → 转 B 档流程；选 3 → 完整外观描述 + 强确认，提示词用条件式，并明确告知风险。

---

## 8.5 阶段 S：合成阶段（新增：视频模型不支持多参考图时）

**触发条件**：选定视频模型 `multi_ref_supported=false`（查 models.json）——模型只吃单张首帧图，无法直接吃多张参考图。

**逻辑**：用生图模型（开放接入，image_client.py）把「商品 + 模特/场景」合成一张首帧图，再图生视频。

```
视频模型支持多参考图 → 直接图生视频（多参考图+提示词）——原流程
视频模型只吃单图 → S 合成阶段：
  ① 输入：商品白底图 + 使用/佩戴图(模特) 或 场景图 + 合成提示词
  ② 生图模型合成首帧（商品穿在模特身上/置于场景中，光影与场景自洽）
  ③ 合成图质检（外观比对 vs 商品图：版型/色值/图案/五金）→ 不合格重生成
  ④ 合成图作为唯一参考 → 图生视频
```

**合成提示词**：AI 引擎 image_gen 子模式生成（非拼接）：
- L1 商品保真：与参考商品图完全一致（版型/色值/印花/五金）
- L2 融合：穿在模特身上/置于场景中，光影与场景图自洽
- L3 禁区：不改商品外观、不遮挡关键细节

**双重质检（合成图是视频唯一外观锚点）**：
```
第一道：合成图 vs 商品白底图（版型/色值/图案/五金）
第二道：视频 vs 合成图
任何一道不过 → 回合成阶段重生成，不盲目调视频提示词
```

**配置**：`.env` 的 `IMAGE_PROVIDER` + `IMAGE_API_KEY/BASE/MODEL`（开放接入，默认 custom）；生成物归档 `assets/{sku}/composite/`。

---

## 9. 阶段 D：细节确认门（已定：独立确认单，一商品一页）

输出《细节确认清单》，**每个商品生成一页独立确认单**（含商品名、日期、逐项内容、确认状态）。批量单强制使用，单商品小单可简化。

```
【细节确认单 · 商品名】日期：____
┌─────────────────────────────────────────────┐
│ 1. 材质/关键外观 : 丝绸（动态锚定：垂坠流动，光泽滑动） │ ← 来源：识别+确认/文字
│ 2. 外观一致性   : 与参考图一致（不写参数）            │ ← 来源：参考图
│ 3. 颜色/主视觉  : 与参考图一致（不写色值）            │ ← 来源：参考图
│ 4. 图案/细节    : 与参考图一致（不描述纹样）          │ ← 来源：参考图特写
│ 5. 关键细节     : 与参考图一致（不描述数量样式）      │ ← 来源：参考图特写
│ 6. 动态层       : 转身360° + 裙摆甩动 + 液态光泽      │ ← 提示词主力
│ 7. 风格类型     : ①TVC 电影感大片（03 §7 锁定）       │ ← 类型推荐
└─────────────────────────────────────────────┘
请回复：全部正确 / 或指出修改项（例：第1项改为棉）
```

> 品类化：第 1 行按品类知识包换（美妆=色号/质地；食品=形态/色泽；3C=外壳材质/屏幕）。

规则：
- 来源"参考图"的项：客户确认参考图没问题即可；来源"识别+确认/文字"的项：逐字检查
- 客户全部确认前，不调用生成
- 确认单留存归档（多 SKU 不混淆、责任有据可查）

---

## 10. 阶段 E：质检闭环（外观/材质比对 + 品类质检）

02 模板 8 项质检基础上新增：

- [ ] **外观/材质比对**：视频中产品外观、材质动态表现是否符合确认单（服装=面料动态；美妆=质地妆效；食品=形态色泽；3C=外壳质感）
- [ ] **品类质检项**：查 `knowledge/profiles/{品类}.profile.json → M9_qa_standards`（14 品类各 7-15 项专业验收清单，如母婴的宝宝形象一致性=关键项、珠宝的金属颜色比对）
- [ ] **合规 4 项**：查 `knowledge/compliance.json → video_specific_checks`（宣称/对比/数据/品类红线）
- [ ] 错判 → 回查：参考图缺位（→补图）还是描述缺位（→补 L3 锚定）？修对应通道，不盲目加描述

---

## 11. 阶段 F：批量生成衔接（已定：确认单 = 队列入场券）

- **没有全确认的确认单，提示词不允许进入 Seedance 生成队列**（机制上堵死"没确认就生成"）
- 多商品确认节奏：
  - **1-3 个商品：串行**（逐个确认→生成，稳）
  - **4 个以上：并行**（全部发确认单，客户逐页确认，统一进队列，快）
- 队列按"确认单完成状态"驱动，不按"客户给了需求"驱动

---

## 12. 防材质错判专项演练（通用版）

### 示例一：服装（丝绸 vs 纯棉）

场景：客户给 1 张白底吊带裙图，未标注面料。生成后裙子"像纯棉一样硬挺小幅度摆动"。

排查顺序：

```
① 参考图特写缺失？          → 补面料特写图，重走 B/D
② 提示词缺 L3 材质锚定？    → 加"丝绸面料，垂坠如液体流动"
③ 材质确认是否被跳过？      → 补确认，以后禁止跳过（B2 追问机制）
④ 提示词与参考图冲突？      → 删冲突描述
```

### 示例二：美妆（口红颜色偏移）

场景：客户给 1 张口红白底图。生成后口红色号变了、膏体像唇膏笔。

排查顺序：

```
① 特写图缺失（无色号特写）？  → 补色号特写图，重走 B/D
② 提示词缺 L3 锚定（色号/质地）？ → 加"正红色膏体，丝绒哑光质地"
③ 确认门是否确认过色号？      → 补确认（美妆命门=色号+质地）
④ 描述与参考图冲突？          → 删冲突描述（如误写"滋润水光"而实际是哑光）
```

### 通用排查口诀

```
特写缺失 → 补图；锚定缺失 → 补 L3；确认跳过 → 补确认；描述冲突 → 删冲突
（四查顺序固定，修对应通道，不盲目加描述）
```

正确链路（通用）：

```
收图 → B1 识别：图类型(高) + 品类(高) + 材质推断(中，依据：光泽/质感/纹理)
→ B2 确认：客户"全部正确" → 追问"材质确认是【推断值】吗？" → 确认
→ C 自动分档 → D 确认单（材质行=识别+确认）→ 客户"全部正确"
→ 拼装：L1 与参考图完全一致 + L2 动作/场景/光线 + L3 材质动态锚定
→ 生成 → 质检外观/材质比对 → 通过
```

---

## 13. 决策记录（v1.1）

| 项 | 决策 |
|---|---|
| 1 识别报告形式 | **文本表格**，客户**自然语言整体回复**（"全部正确"或指出修改项）；面料推断值不因"全部正确"默认确认，需追问一次 |
| 2 档位判定 | **自动查表**（确认后锚点完整度 → 档位，纯规则） |
| 3 C 档无参考图 | **询问式，按客户指令操作**（补真实图 / AI样衣概念图转B档 / 坚持文字生成风险自负） |
| 4 细节确认门 | **独立确认单，一商品一页**，批量单强制、含归档 |
| 5 批量衔接 | **确认单=队列入场券**；1-3 商品串行，4+ 并行发确认单 |
| 6 类型推荐（v1.1 新增） | **商品特性驱动打分推荐**：品类/面料/价格带/场合/季节/调性六维匹配，硬约束锁定+打分排序输出 Top3+理由；客户确认锁定后写入确认单；换类型需重走分镜骨架 |

## 14. 与现有资产的关系

```
projects/服装电商AI视频/
├── 00-项目进度快照.md       ← 上下文对齐基准
├── 01-面料×场景×光线对照表.md ← 知识库：L2/L3 层词源（服装）
├── 02-分镜表模板.md         ← 输出层：信息卡+分镜表+质检（镜头骨架查 profiles M11）
├── 03-本文（v1.2）         ← 输入逻辑层（含类型推荐规则）
├── 04-客户问答话术与确认流程.md ← 对客执行层
├── 05-电商广告视频类型库.md  ← 风格能力清单 + 六维权重表
├── 06-品类合规红线表.md     ← 全品类合规底线
└── knowledge/              ← JSON 知识库（index.json 检索入口）
    ├── profiles/            ← 14品类 × 11模块 行业级拍摄知识（M1-M11）
    ├── *-scene-light.json   ← 14品类场景光线对照
    ├── types.json           ← 12类型+六维权重
    ├── category-profiles.json / compliance.json / vocab.json
    └── PROCESS.md / BLUEPRINT.md / README.md
```
