# 环境变量配置规范（.env）

> 所有密钥/模型配置统一走 `.env`，禁止硬编码在代码/提示词/文档里。
> 模板：`.env.example`（复制为 `.env` 后填写）。
> 目标：避免调用时因密钥省略导致的 401/403 含糊失败——接单前先做配置检查。

---

## 1. 文件位置

| 文件 | 说明 |
|---|---|
| `.env.example` | 模板（可提交版本库） |
| `.env` | 真实密钥（**不提交版本库**，加入 .gitignore） |
| `knowledge/models.json` | 模型能力注册表（不含密钥，与 .env 分离） |

## 2. 变量分组

| 组 | 前缀 | 用途 |
|---|---|---|
| 视觉识别 | `VISION_*` | 阶段 A1 参考图识别（GPT-4o/Gemini/Claude） |
| 视频生成 | `VIDEO_PROVIDER` + 各模型 `{MODEL}_API_KEY/BASE/MODEL` | 阶段 A2 生成模型（开放接入） |
| 网络 | `HTTP_PROXY/HTTPS_PROXY/API_TIMEOUT/API_RETRY*` | 代理与重试 |
| 路径 | `KNOWLEDGE_DIR/OUTPUT_DIR` | 项目路径 |

## 3. 加载规则（按优先级）

```
1. 环境变量（系统已设置的优先）
2. .env 文件（项目根目录）
3. 缺省值（见下表）
```

| 变量 | 缺省 | 缺失后果 |
|---|---|---|
| VISION_API_KEY | 空 | **走人工模式**（识别由人工转录），并提示客户 |
| VIDEO_PROVIDER | seedance-2.0 | 按默认模型处理 |
| SEEDANCE_API_KEY | 空 | 生成前必检，缺失则提示补配，**不发起调用** |

## 4. 接单前配置检查（强制节点，对应 03 阶段 A）

开始任何商品前，先执行配置自检（可脚本化）：

```
检查项：
[ ] VISION_API_KEY 已配置？  → 否 → 提示客户接入或确认走人工模式
[ ] VIDEO_PROVIDER 有效？    → 否 → 回退默认 seedance-2.0
[ ] {VIDEO_PROVIDER}_API_KEY 已配置？ → 否 → 提示补配，暂不进入生成
[ ] {VIDEO_PROVIDER} 在 models.json 有注册？ → 否 → 按 custom 保守参数 + 风险标注
[ ] 密钥格式校验（非空、无空格/换行残留）
```

**防密钥省略失败的设计**：
- 密钥缺失时**前置拦截**（接单/入队前报"未配置 X 密钥，请补 .env"），而不是调用时才报 401/403
- 密钥从 .env 注入调用层，**不随提示词/分镜 JSON/确认单传递**（避免复制粘贴时截断、日志泄露）
- 每次调用前校验非空 + 基础格式；连续失败按 API_MAX_RETRIES 重试

## 5. 安全约定

- `.env` 加入 .gitignore；模板 `.env.example` 可提交（只含变量名不含值）
- 日志/报错信息**脱敏**：只显示 key 前缀 4 位（如 `sk-1a2b…`），不打印完整密钥
- 确认单/分镜表等交付物不含任何密钥字段
